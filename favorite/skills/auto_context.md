# auto_context skill

  Автоматически находит релевантные файлы проекта по ключевым словам и инжектирует их фрагменты в контекст.

  ## Использование

  ```
  <SKILL:name=auto_context>registry command</SKILL>
  ```

  Возвращает топ-5 файлов с фрагментами (до 500 символов каждый).
  