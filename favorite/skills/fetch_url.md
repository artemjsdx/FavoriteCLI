# fetch_url

  Скачивает страницу по URL и возвращает чистый текст (без HTML-тегов).

  ## Вызов

  ```
  <SKILL:name=fetch_url>https://example.com/page</SKILL>
  ```

  ## Возвращает

  Текст страницы (до 6000 символов). Если страница длиннее — обрезается.
  