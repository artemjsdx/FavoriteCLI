Напиши модуль favorite/utils/retry.py — утилита retry с exponential backoff.

  Требования:
  - функция retry(fn, max_attempts=3, base_delay=1.0, exceptions=(Exception,))
  - поддержка async функций
  - логирование каждой попытки
  - тесты в favorite/utils/test_retry.py (минимум 4 теста)

  ВАЖНО: После написания — проверь сам себя:
  Задание выполнено полностью если:
    - файл retry.py создан и импортируется без ошибок
    - файл test_retry.py создан и все тесты проходят (python -m pytest)
    - покрыты случаи: успех с первой попытки, успех после retry, исчерпание попыток, async функция

  Если проверка говорит что что-то не сделано — доделай и проверь снова.
  Используй независимого наблюдателя для объективной проверки результата.
  