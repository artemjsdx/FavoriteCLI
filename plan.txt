# Agents System — План разработки

  ## Текущее состояние
  - Суб-агенты: запуск через /agents spawn <id> <задача>
  - Список ролей из sub_roles_library.json (12 ролей)
  - OpenRouter — единственный провайдер для суб-агентов (FavoriteAPI не поддерживался)
  - Нет интерактивного меню, нет редактирования агентов

  ## Задачи

  ### T1: Интерактивное меню /agents [DONE]
  - Нумерованный список: main-1 + суб-агенты
  - Выбор по номеру — подменю
  - Запуск суб-агента из меню (без ручного ввода команды)

  ### T2: Редактор main-1 [DONE]
  - Изменить имя (config/user_agents.json)
  - Доп. системный промпт (config/user_agents.json)
  - Статус active/inactive
  - Модель/провайдер → редирект на /OpenRouter API или /Favorite API

  ### T3: FavoriteAPI для суб-агентов [DONE]
  - sub_agent.py: OpenRouter → если нет → FavoriteAPI → если нет → ошибка с подсказкой
  - В меню: предупреждение с выбором если нет ни одного API

  ### T4: Главный агент-помощник [FUTURE]
  - Статус: НЕ реализован (крупная фича, отдельная задача)
  - Концепция: постоянный агент рядом с main-1, своя история, параллельный поток
  - Требует: threading/async, отдельный LLM поток, протокол обмена

  ## Структура данных

  config/user_agents.json:
  {
    "main": {
      "name": "Главный агент",
      "active": true,
      "custom_prompt": null
    },
    "custom_roles": []
  }
  